# dependencies
node_modules/

# build output
dist/
.astro/
.vercel/
.output/

# local database — anchored to the repo root, or it also swallows
# src/data/ (the baked arm) and public/data/ (the baked scan)
/data/
*.sqlite
*.sqlite-*
*.db
*.db-*

# secrets — never commit
.env
.env.*
!.env.example

# editor / os
.DS_Store
.idea/
.vscode/
*.log

# fetched dataset frames (regenerate with npm run lidar:fetch)
.cache/
