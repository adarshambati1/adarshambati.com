{
  "name": "adarsh-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "icons": "node scripts/make-icons.mjs",
    "preview": "astro preview",
    "verify": "astro check && astro build && node scripts/test-arm-safety.mjs",
    "test:arm": "node scripts/test-arm-safety.mjs",
    "lidar:fetch": "node scripts/fetch-pandaset-frame.mjs",
    "lidar:bake": "python3 scripts/bake-pandaset-frame.py",
    "test": "bash scripts/smoke.sh"
  },
  "dependencies": {
    "@astrojs/vercel": "^11.0.5",
    "@fontsource-variable/hanken-grotesk": "^5.3.0",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
